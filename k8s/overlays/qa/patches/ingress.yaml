apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-app
spec:
  rules:
    - host: qa.crm.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: qa-crm-app
                port:
                  name: http
  tls:
    - hosts:
        - qa.crm.example.com
      secretName: crm-tls-qa
