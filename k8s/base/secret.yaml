# ============================================================================
# TEMPLATE ONLY — do NOT commit real values.
# In production, use:
#   - Sealed Secrets (kubeseal)
#   - External Secrets Operator (AWS SM / GCP SM / Vault)
#   - SOPS + age/kms
#
# Real secrets are injected via CI/CD from GitHub Secrets / Vault.
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: crm-app-secrets
  labels:
    app.kubernetes.io/name: crm-app
    app.kubernetes.io/part-of: crm-saas
type: Opaque
stringData:
  MONGODB_URI: "REPLACE_VIA_CI"
  JWT_SECRET: "REPLACE_VIA_CI"
  RESEND_API_KEY: "REPLACE_VIA_CI"
  MINIO_ACCESS_KEY: "REPLACE_VIA_CI"
  MINIO_SECRET_KEY: "REPLACE_VIA_CI"
  N8N_API_KEY: "REPLACE_VIA_CI"
  API_KEY_ENCRYPTION_SECRET: "REPLACE_VIA_CI"
  GOOGLE_OAUTH_CLIENT_ID: "REPLACE_VIA_CI"
  GOOGLE_OAUTH_CLIENT_SECRET: "REPLACE_VIA_CI"
  RAZORPAY_KEY_ID: "REPLACE_VIA_CI"
  RAZORPAY_KEY_SECRET: "REPLACE_VIA_CI"
  DODO_API_KEY: "REPLACE_VIA_CI"
  DODO_WEBHOOK_SECRET: "REPLACE_VIA_CI"
