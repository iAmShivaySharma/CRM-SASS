apiVersion: v1
kind: Service
metadata:
  name: crm-app
  labels:
    app.kubernetes.io/name: crm-app
    app.kubernetes.io/part-of: crm-saas
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: crm-app
